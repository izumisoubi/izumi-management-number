# Supabase Edge Functions

No Edge Function source is currently present in this repository.

When an Edge Function is introduced, its complete source, dependency lockfile, environment-variable names, deployment command and PostgreSQL fallback or replacement plan must be committed under this directory. Secrets must remain in the deployment environment and must never be committed.
